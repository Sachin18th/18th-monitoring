{
  "name": "@kpi-platform/api",
  "version": "1.0.0",

  "main": "dist/server.js",
  "types": "dist/server.d.ts",

  "scripts": {
    "dev": "tsx src/server.ts",
    "build": "tsc",
    "build:tracker": "esbuild src/public/tracker.src.js --bundle --minify --format=iife --target=es2017 --legal-comments=none --outfile=src/public/tracker.js",
    "start": "node dist/server.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src --ext .ts"
  },

  "dependencies": {
    "@fastify/cors": "^8.5.0",
    "@kpi-platform/db": "*",
    "dotenv": "^16.0.0",
    "fastify": "^4.0.0",
    "@prisma/client": "^5.22.0",
    "nodemailer": "^6.9.0",
    "pino": "^9.0.0",
    "zod": "^3.0.0"
  },

  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/nodemailer": "^6.4.0",
    "@types/supertest": "^7.2.0",
    "@vitest/coverage-v8": "^4.1.5",
    "esbuild": "^0.27.0",
    "supertest": "^7.2.2",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "vitest": "^4.1.5"
  }
}